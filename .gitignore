.DS_Store
Thumbs.db
*.log
node_modules/
.vscode/
.idea/
.claude/

# Capacitor build outputs (regenerated by `npm run build`)
www/

# Generated native Android project. We commit the templates in
# android-templates/ instead, then drop them into android/ after
# `npx cap add android` is run locally. Keeps the repo clean.
android/
package-lock.json
